<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>File Details</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body class="flex flex-col min-h-screen bg-background">
    <%- include("../../partials/header") %>
    <main class="flex-1 container max-w-7xl mx-auto p-4">
      <div class="flex items-center justify-center">
        <div
          class="w-full max-w-md bg-secondary/20 border border-secondary/30 rounded-xl shadow p-6"
        >
          <h2 class="text-2xl font-bold mb-6 text-center text-accent">
            File Details
          </h2>

          <dl class="space-y-4 text-sm text-text">
            <div>
              <dt class="font-medium text-secondary">Name</dt>
              <dd class="ml-4 text-base font-semibold text-text">
                <%= file.name %>
              </dd>
            </div>
            <div>
              <dt class="font-medium text-secondary">Size</dt>
              <dd class="ml-4 text-base font-semibold text-text">
                <%= (file.size/1024).toFixed(2) %> KB
              </dd>
            </div>
            <div>
              <dt class="font-medium text-secondary">Uploaded At</dt>
              <dd class="ml-4 text-base font-semibold text-text">
                <%= new Date(file.createdAt).toLocaleString() %>
              </dd>
            </div>
          </dl>

          <div class="mt-8 flex justify-center">
            <a
              href="/folders/<%= folder.id %>/<%= file.id %>/download"
              download
              class="px-5 py-2 bg-accent text-white rounded-md text-sm hover:bg-accent/80 transition"
            >
              Download
            </a>
          </div>
        </div>
      </div>
    </main>
    <%- include("../../partials/footer") %>
  </body>
</html>
